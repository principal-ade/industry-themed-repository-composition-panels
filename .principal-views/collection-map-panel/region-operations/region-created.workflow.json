{
  "version": "1.0.0",
  "canvas": ".principal-views/collection-map-panel/collection-map-panel.otel.canvas",
  "name": "Region Created",
  "description": "User creates a new region on the collection map",
  "spanPattern": "collection-map.region-created",
  "scenarios": [
    {
      "id": "region-created",
      "description": "User created a new region",
      "priority": 1,
      "template": {
        "summary": "Created region '{{region.name}}' in collection {{collection.id}}",
        "events": {
          "collection-map.region-created": "Created region '{{region.name}}' ({{region.id}}) at order {{region.order}} in collection {{collection.id}}"
        }
      }
    }
  ]
}
