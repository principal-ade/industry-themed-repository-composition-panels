{
  "version": "1.0.0",
  "canvas": ".principal-views/collection-map-panel/collection-map-panel.otel.canvas",
  "name": "Region Deleted",
  "description": "User deletes a region from the collection map",
  "spanPattern": "collection-map.region-deleted",
  "scenarios": [
    {
      "id": "region-deleted",
      "description": "User deleted a region",
      "priority": 1,
      "template": {
        "summary": "Deleted region '{{region.name}}' from collection {{collection.id}}",
        "events": {
          "collection-map.region-deleted": "Deleted region '{{region.name}}' ({{region.id}}) from collection {{collection.id}}, {{nodes.orphaned}} nodes orphaned"
        }
      }
    }
  ]
}
